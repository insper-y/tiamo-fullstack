/* ============================================================
   Tiamo AI 前端配置文件
   部署到 GitHub Pages 后，修改这里的 API_BASE 为你的后端地址
   ============================================================ */

window.APP_CONFIG = {
    // 后端 API 地址
    // 本地开发: http://localhost:8080
    // Render 部署: https://tiamo-backend.onrender.com
    API_BASE: 'https://hwiwdlrhrbiy.sealosbja.site',

    // 网站名称
    SITE_NAME: 'Tiamo AI',

    // Token 存储 key
    TOKEN_KEY: 'tiamo_token',
    USER_KEY: 'tiamo_user'
};
